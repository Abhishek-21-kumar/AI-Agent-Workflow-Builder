# Nhost Serverless Functions / Hasura Actions

This directory will house Nhost serverless Node.js / TypeScript functions and HTTP webhooks for future phases:
- Webhook trigger endpoint listener (`/functions/v1/trigger`)
- Scheduled workflow cron handlers
- Async LLM call execution engine & approval gate resolution endpoints
